const ModalDefaultPage = () => {
  // we are returning null here so that no visable content will me rendered
  // we only want to render the interceptor page that will open with app routing
  return null;
};

export default ModalDefaultPage;
